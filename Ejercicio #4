ef primo(numero):
    if numero < 2:
        return False
    for x in range(2, numero):
        if numero % x == 0:
            return False
    return True

numero=int(input("Ingrese un número: "))
if primo(numero):
    print("Es primo")
else:
    print("No es primo")
